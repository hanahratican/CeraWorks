const User = require('./User');
// for some reason vs code wants to change this to const Schedule = require('./schedule'); with a lowercase s
const Schedule = require('./schedule');
const Review = require('./Review');

module.exports = { User, Schedule, Review };